<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Resume | Rettend</title>
    <style is:global>
      @media print {
        @page {
          size: A4;
          margin: 12mm;
        }
        html,
        body {
          height: auto !important;
          overflow: visible !important;
          background: #fff !important;
          color: #111 !important;
        }
        .dark {
          color-scheme: light;
        }

        .text-card-foreground {
          color: #111 !important;
        }

        [class~='text-muted-foreground'],
        [class*='text-muted-foreground'],
        [class~='text-foreground'],
        [class*='text-foreground/'] {
          color: #111 !important;
        }
        .bg-card {
          background: #fff !important;
        }
        .border {
          border-color: #00000022 !important;
        }
        .op-50,
        .op-60,
        .op-70 {
          opacity: 1 !important;
        }
        a {
          color: #222 !important;
          text-decoration: underline !important;
        }

        .no-print {
          display: none !important;
        }
        .bg-speckles {
          display: none !important;
        }
        .avoid-break {
          break-inside: avoid;
          page-break-inside: avoid;
        }
        .section-title {
          break-after: avoid;
        }

        .resume-projects .resume-project-item {
          background: transparent !important;
          padding: 6px 8px !important;
        }
        .resume-projects ul > li + li {
          margin-top: 6px !important;
        }
        .resume-projects section {
          margin-top: 0.75rem !important;
        }
        .resume-projects h3 {
          margin-top: 0 !important;
          margin-bottom: 0.25rem !important;
          font-size: 0.9rem !important;
          letter-spacing: 0.03em !important;
        }
        .resume-projects h3 + ul {
          margin-top: 0.25rem !important;
        }
        .resume-projects .resume-project-item > p {
          margin-top: 2px !important;
          line-height: 1.2 !important;
        }
        .resume-projects .tags-list {
          margin-top: 0.25rem !important;
          gap: 0.25rem !important;
        }
        .resume-projects .tags-list span {
          border: 0 !important;
          padding: 0 !important;
          border-radius: 0 !important;
          font-size: 10px !important;
          color: #333 !important;
        }
        .resume-projects .tags-list span + span::before {
          content: ' · ';
          color: #555;
        }
        .resume-projects .tags-list span:nth-child(n + 5) {
          display: none !important;
        }
      }
    </style>
  </head>
  <body class="dark font-sans flex flex-col h-dvh">
    <div class="fixed inset-0 -z-10 bg-speckles"></div>
    <div class="flex-grow scroll-smooth scrollbar-gutter-stable overflow-y-auto flex flex-col">
      <slot />
    </div>
  </body>
</html>
